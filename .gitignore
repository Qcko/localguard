# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv
SESSION.md

# Calibration suite -- cached real-package tarballs are large (~300MB) and
# generated. Run tools/seed_calibration_cache.py to populate locally.
tests/calibration/data/
tests/calibration/data_index.json

# Claude Code runtime state (lock files, transient scheduled-task bookkeeping).
.claude/scheduled_tasks.lock
.claude/worktrees/

# One-off prompt artifacts pasted in for a single session.
PROMPT-from-*.md
